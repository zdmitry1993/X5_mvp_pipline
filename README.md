🎯 Этап 1: Python ETL (Локально)

Python скрипт читает CSV (10K транзакций)
Имитирует Flink: оконные агрегации через Pandas
Сохраняет результат в Parquet
Имитирует Iceberg (метаданные JSON)
Выводит бизнес-инсайты

Архитектура:

CSV → Pandas (Flink-логика) → Parquet + JSON метаданные
Результат: sales_daily.parquet + аналитика

🚀 Этап 2: Docker инфраструктура (Selectel)

Развернул в облаке: MinIO + PostgreSQL + Trino + Nginx
Настроил Trino с тестовыми каталогами
Создал публичные endpoints

Что работает:

docker-compose инфраструктура → Trino с каталогами:
- tpch (демо-данные)
- memory (таблицы в памяти)  
- system, jmx
  
Доступно:

Trino UI: http://IP:8080 (SQL запросы)
MinIO: http://IP:9001 (готов для данных)
Демо-сайт: http://IP

🔗 Связь этапов:

Сейчас:

Этап 1: Parquet файлы (готовы)
Этап 2: Инфраструктура (готова)
Для полного цикла:

Parquet → MinIO → Trino таблицы → SQL аналитика
(интеграция не сделана, но инфраструктура готова)

